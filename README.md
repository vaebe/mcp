# mcp

## 版本更新

```bash
# 安装 changesets
npm install @changesets/cli --save-dev

# 初始化 changesets
npx changeset init

# 创建一个新的 changeset
npx changeset

# 更新版本号和生成发布日志
npx changeset version

# 发布
npm publish
```